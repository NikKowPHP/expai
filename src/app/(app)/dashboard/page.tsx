// src/app/(app)/dashboard/page.tsx

export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold mb-4">Main Dashboard</h1>
      <p>This is the content for the dashboard page.</p>
      <p>The sidebar on the left is persistent and shared across all pages in the `(app)` group.</p>
    </div>
  );
}
